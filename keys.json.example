{
  "_comment": "Virtual-SK allowlist template. Copy to keys.json (gitignored) and edit. An absent/empty keys.json means fail-open (auth disabled). Reloaded on change (watchFile) and on SIGHUP. `enabled` defaults true; set false to revoke without deleting. `expires` is an optional ISO-8601 instant; past it the key is rejected. `label` is a human tag; the metric label is always the masked key.",
  "keys": [
    {
      "key": "sk-yc-alice-9f3k2m",
      "label": "alice",
      "enabled": true,
      "expires": "2026-12-31T00:00:00Z"
    },
    {
      "key": "sk-yc-bob-x82mqp",
      "label": "bob",
      "enabled": true
    }
  ]
}
